# ============================================================
# requirements.txt
# A-Vertex Automation Tool — Python dependencies
#
# Install:
#   pip install -r requirements.txt
#   playwright install chromium
# ============================================================

# Browser automation framework
playwright>=1.40.0

# Load credentials from .env file (backward compatible — env vars still work without .env)
python-dotenv>=1.0.0

# File locking to prevent race conditions when multiple users run simultaneously
filelock>=3.13.0